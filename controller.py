from model_matrix import Matrix 
from const import * 


def Initialize (myMatrix :Matrix) : 
    COLS, ROWS = myMatrix.width+1, myMatrix.height+1
    RES = WIDTH, HEIGHT = 800+2*BOUND + (ROWS-1)*(A1), 800+2*BOUND + (COLS-1)*(A1)